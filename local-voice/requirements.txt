# Local AMD-friendly Unmute-shaped voice stack for Cog.
# STT: faster-whisper (CPU int8 — AMD has no CUDA)
# LLM: Ollama (uses your AMD GPU via Vulkan when available)
# TTS: kokoro-onnx (CPU/ONNX, fast enough for sentence streaming)

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0
numpy>=1.26.0
faster-whisper>=1.1.0
kokoro-onnx>=0.4.0
soundfile>=0.12.1
httpx>=0.27.0
onnxruntime>=1.19.0
